# 订单状态机规则表（SSOT · 唯一事实来源）

> **本文件是订单状态与流转规则的唯一定义处。**
> 代码里的枚举、流转表、C 端时间轴、后台下拉框，全部必须与本文件一致。
> 任何不一致 = 代码漂移，必须修到一致为止。
> 来源：《洗车项目PRD V2.0》表 4-1（订单状态机）、4.1/4.2（全链路时序）、9（异常场景）、附录 A（状态流转）。

---

## 一、状态清单（15 个）

| # | 状态值（代码用） | 中文名（展示用） | 终态 | 客户可自助取消 | 说明 |
| --- | --- | --- | --- | --- | --- |
| 1 | `WAIT_PAY` | 待支付 | | ✅ | 订单已创建，15 分钟未支付自动取消 |
| 2 | `WAIT_KEY` | 待存钥匙 | | ✅ | 支付成功且格口预占成功 |
| 3 | `KEY_IN` | 钥匙已入柜 | | ✅ | 柜机回调：存入并关门 |
| 4 | `PICKING` | 取车中 | | ✅ | 工作人员扫码取出钥匙（**车尚未移动**） |
| 5 | `TO_STATION` | 运输中（去程） | | ❌ | 取车拍照完成，车辆驶离 |
| 6 | `WASHING` | 清洗中 | | ❌ | 入场打卡，进入工位 |
| 7 | `QC` | 待质检 | | ❌ | 15 步 SOP 全部完成 |
| 8 | `WAIT_RETURN` | 待还车 | | ❌ | 质检通过，等待送回 |
| 9 | `RETURNING` | 运输中（回程） | | ❌ | 车辆驶离中央站 |
| 10 | `RETURNED` | 已还车（待取钥匙） | | ❌ | 停放拍照 + 钥匙归柜 |
| 11 | `WAIT_REVIEW` | 待评价 | | ❌ | 客户取出钥匙 |
| 12 | `FINISHED` | 已完成 | ✅ | ❌ | 评价完成或 T+3 自动流转 |
| 13 | `CANCELED` | 已取消 | | ❌ | 客户取消 / 运营作废 / 超时 |
| 14 | `REFUNDING` | 退款中 | | ❌ | 退款申请通过 |
| 15 | `REFUNDED` | 已退款 | ✅ | ❌ | 退款成功 |

**终态**：`FINISHED`、`REFUNDED` —— 进入后不可再流转（除退款外，见特殊规则）。

**取消权限**：客户自助取消仅限前 4 态（车还没动）；`TO_STATION` 及之后只能由**客服/站长**在后台取消（PRD 9：已在清洗时禁止客户取回，只展示预计完成时间）。

---

## 二、流转规则表（17 个事件）

> 格式：**起始状态 + 事件 → 目标状态**。表中没有的组合，代码一律拒绝并报错。

### 2.1 正常主链路

| 事件（代码用） | 中文 | 触发方 | 起始 → 目标 | 强制校验 / 副作用 |
| --- | --- | --- | --- | --- |
| `PAY_SUCCESS` | 支付成功 | 支付回调 / 查单补偿 | `WAIT_PAY` → `WAIT_KEY` | 绑定格口、扣产能名额、推订阅消息「请存钥匙」 |
| `DEPOSIT_KEY` | 客户存放钥匙 | 柜机回调 | `WAIT_KEY` → `KEY_IN` | 写开箱记录、推「钥匙已收到」 |
| `TAKE_KEY` | 工作人员取钥匙 | 取送端 | `KEY_IN` → `PICKING` | 校验人单绑定、写开箱记录、生成取车任务 |
| `PICK_CAR_DONE` | 取车拍照完成 | 取送端 | `PICKING` → `TO_STATION` | **必须 ≥6 张 360° 照片 + 仪表盘**，否则拒绝 |
| `ARRIVE_STATION` | 入场打卡 | 作业端 | `TO_STATION` → `WASHING` | 分配工位、生成洗车任务 |
| `SOP_DONE` | SOP 全部完成 | 作业端 | `WASHING` → `QC` | 15 步全部打卡，否则拒绝 |
| `QC_FAIL` | 质检不合格 | 作业端 | `QC` → `WASHING` | 必填原因；重洗次数 +1；**≥2 次自动生成复盘工单** |
| `QC_PASS` | 质检通过 | 作业端 | `QC` → `WAIT_RETURN` | 上传洗后照、自动拼前后对比图、推「清洗完成」 |
| `LEAVE_STATION` | 驶离中央站 | 取送端 | `WAIT_RETURN` → `RETURNING` | 记录出场时间 |
| `RETURN_DONE` | 停放完成 + 钥匙归柜 | 取送端 / 柜机回调 | `RETURNING` → `RETURNED` | 停放照（含车位号）、归柜留痕、推取件码（短信兜底） |
| `TAKE_KEY_BACK` | 客户取回钥匙 | 柜机回调 | `RETURNED` → `WAIT_REVIEW` | 记录取钥匙时间，用于准点还车率考核 |
| `REVIEW_SUBMIT` | 提交评价 | C 端 | `WAIT_REVIEW` → `FINISHED` | ≤4 星强制填原因并自动生成质检复盘工单 |
| `AUTO_FINISH` | 超时自动完成 | 定时任务 T+3 | `WAIT_REVIEW` → `FINISHED` | — |

### 2.2 顺延（自环）

| 事件 | 触发方 | 起始 → 目标 | 说明 |
| --- | --- | --- | --- |
| `POSTPONE` | 定时任务 23:30 | `WAIT_KEY` → `WAIT_KEY` | 状态不变，改预约日期；**释放格口重分配、返还券/体验名额、推送客户** |

### 2.3 取消（13 条）

`CANCEL` 事件允许的起始状态：**除 `FINISHED` / `CANCELED` / `REFUNDING` / `REFUNDED` 之外的全部 11 个状态** → `CANCELED`。

- 触发方 = 客户：仅允许 `WAIT_PAY` / `WAIT_KEY` / `KEY_IN` / `PICKING`，其余拒绝。
- 触发方 = 客服 / 站长 / 定时任务：11 个状态均可。
- 必填取消原因，写入流转日志。
- 副作用：释放格口、回补产能名额、权益（券/体验名额）返还、自动触发退款流程。

### 2.4 退款（4 条）

| 事件 | 触发方 | 起始 → 目标 | 说明 |
| --- | --- | --- | --- |
| `APPLY_REFUND` | C 端售后 / 客服 | `CANCELED` → `REFUNDING` | 未取车取消退款 |
| `APPLY_REFUND` | C 端售后 / 客服 | `WAIT_REVIEW` → `REFUNDING` | 售后全额退款（洗不干净重洗仍不满意） |
| `APPLY_REFUND` | C 端售后 / 客服 | `FINISHED` → `REFUNDING` | 售后全额退款 |
| `REFUND_SUCCESS` | 支付回调 | `REFUNDING` → `REFUNDED` | 原路退回成功，推送客户 |

---

## 三、特殊规则（容易踩坑，写死在这里）

1. **部分退款不改状态**：仅生成退款单（`wash_refund`），订单状态保持不变，仍走 `FINISHED`。只有**全额退款**才流转到 `REFUNDING → REFUNDED`。
2. **质检回退不新增状态**：`QC → WASHING` 是回退，用 `rewash_count` 字段记次数，不单独设"重洗中"状态。
3. **顺延是自环**：`WAIT_KEY → WAIT_KEY`，只改预约日期，不要把它设计成新状态。
4. **超时自动取消**：`WAIT_PAY` 超 15 分钟（参数可配）由定时任务发 `CANCEL`，不是直接改状态。
5. **超时自动完成**：`WAIT_REVIEW` 满 T+3 天由定时任务发 `AUTO_FINISH`。
6. **`TO_STATION` 与 `RETURNING` 是两个不同状态**：都显示"运输中"，但一去一回，C 端文案不同（去程/回程），不能合并。
7. **每一次流转都必须写一条 `wash_order_status_log`**，只增不删，C 端 8 节点时间轴直接读它，不另写一套进度。

### C 端 8 节点时间轴映射（PRD 6.1.3 P9）

| 时间轴节点 | 对应状态 | 展示证据 |
| --- | --- | --- |
| 已下单 | `WAIT_PAY` 之后任意 | 订单号 |
| 钥匙已入柜 | `KEY_IN` | 开箱记录（人/时/格号） |
| 已取车 | `PICKING` / `TO_STATION` | ≥6 张 360° 照片 + 仪表盘 |
| 到达中央站 | `WASHING` 起 | 入场时间、工位号 |
| 清洗中 | `WASHING` | SOP 15 步进度 |
| 质检完成 | `WAIT_RETURN` 起 | 洗后照 + 前后对比图 |
| 已还车 | `RETURNED` | 停放照 + 归柜记录 |
| 待取钥匙 | `RETURNED` → `WAIT_REVIEW` | 取件码 |

---

## 四、落库

```sql
CREATE TABLE wash_order_status_log (
  id            BIGINT       PRIMARY KEY AUTO_INCREMENT,
  order_id      BIGINT       NOT NULL                COMMENT '订单ID',
  from_status   VARCHAR(32)                          COMMENT '流转前状态',
  to_status     VARCHAR(32)  NOT NULL                COMMENT '流转后状态',
  event         VARCHAR(32)  NOT NULL                COMMENT '触发事件（PRD附录D未含，建议新增以便追溯）',
  operator_type VARCHAR(16)  NOT NULL                COMMENT '操作者类型 CUSTOMER/PICKER/STATION/ADMIN/JOB/DEVICE',
  operator_id   BIGINT                               COMMENT '操作者ID（客户/员工/管理员；JOB、DEVICE可为空）',
  source        VARCHAR(16)  NOT NULL                COMMENT '来源端，与 operator_type 同源，避免两套枚举漂移',
  reason        VARCHAR(255)                         COMMENT '原因/备注（取消、质检不合格必填）',
  create_time   DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  KEY idx_order (order_id, create_time)
) COMMENT='订单状态流转日志（只增不删）';
```

---

## 五、代码文件清单

| 文件 | 作用 |
| --- | --- |
| `OrderStatus.java` | 15 个状态的唯一名单（含终态、客户可自助取消标记） |
| `OrderEvent.java` | 17 个事件的唯一名单 |
| `OrderOperatorType.java` | 触发方枚举（客户/取送/作业/后台/定时任务/设备） |
| `OrderStatusTransitions.java` | **流转规则表**：`起始状态 + 事件 → 目标状态`，启动时自校验 |
| `OrderStateMachine.java` | **闸机**：加锁 → 读当前状态 → 校验合法性 → CAS 更新 → 通知副作用 |
| `OrderStatusLog.java` | 流转日志实体（对应上表） |
| `OrderStatusException.java` | 非法流转异常 |

路径：`carwash-server/ruoyi-wash/wash-common/src/main/java/com/ruoyi/wash/common/statemachine/`

## 六、以后要改状态机，只走这三步

1. 先改**本文件**（规则表）；
2. 再改 `OrderStatusTransitions.java`（代码规则表，必须与本文件逐条一致）；
3. 如涉及新增/删除状态，最后改 `OrderStatus.java` 枚举，并同步 C 端时间轴映射与后台字典。

**禁止**：在业务代码里直接 `update status` 或写状态字符串字面量。

---

## 七、已确认的业务决策（2026-09-16 定稿）

| # | 决策 | 结论 | 代码落点 |
| --- | --- | --- | --- |
| 1 | `FINISHED`（已完成）之后能否退款 | **能**。售后入口在 `FINISHED` / `WAIT_REVIEW` 可见，走 `APPLY_REFUND` → `REFUNDING` → `REFUNDED` | `OrderStatusTransitions`：`FINISHED + APPLY_REFUND → REFUNDING` |
| 2 | `PICKING`（已取到钥匙、车未动）时客户取消 | **退全款**，不扣任何跑腿费 | 取消即触发全额退款，不引入“部分退款比例”参数 |
| 3 | 日志表是否新增 `event` 字段 | **新增**。记录触发事件，便于事后追溯“为什么流转” | `OrderStatusLog.event` + 建表 DDL 已含该字段 |

> 上述决策若后续变更，必须先改本表再改代码，并在修订记录中留痕。

## 八、历史待确认项（已关闭）

1. **`FINISHED` 之后还能退款吗？** 我按"能"实现（PRD 售后入口在 `FINISHED`/`WAIT_REVIEW` 可见）。若你认为订单完成 7 天后不再受理，告诉我，我加时间限制。
2. **`PICKING`（工作人员已取到钥匙、但车还没动）时客户取消，退全款吗？** 我按"退全款"实现。若想扣少量跑腿费，需要新增"部分退款比例"参数。
3. **日志表新增 `event` 字段**（PRD 附录 D 没有）：我强烈建议加，否则事后只能看到"从 A 到 B"，看不出"为什么"。确认后我会同步更新 PRD 附录 D 的说明。
