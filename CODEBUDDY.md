# CarGlow 项目协作规则（AI 必读）

> 本文件是 AI 协作的**唯一行为契约**。每次会话开始必须先读本文件，再动手改代码。
> 目的：防止迭代过程中出现代码漂移（重复实现、擅自重构、风格摇摆、破坏既有约定）。

## 1. 项目上下文

夜间代客洗车平台：**中央站集中洗 + 社区智能钥匙柜**。
范围：① C 端微信小程序 ② 智能钥匙柜物联 ③ 师傅端小程序（取送+作业合并） ④ 若依运营后台。

**文档索引（按优先级，冲突时以靠前者为准）**

| 优先级 | 文档 | 内容 |
| --- | --- | --- |
| 0 | `PROGRESS.md` | **跨设备/新会话交接**：当前进度、已确认决策、下一步待办 |
| 1 | `洗车项目PRD.docx` | 业务需求、状态机、页面交互、验收标准 |
| 2 | `洗车项目商业计划书.docx` | 成本与经营约束（MVP 10.5 万、45 天开业、日均 7 单保本） |
| 3 | `洗车项目技术方案.md` | 架构、选型、数据模型、接口规范、ADR |
| 4 | `订单状态机规则表.md` | 订单 15 态与 17 个流转事件的唯一定义（SSOT） |
| 5 | `openapi.yaml` | 前后端接口契约（SSOT），前端 TS 类型与后端 DTO 由它生成 |
| 6 | 代码中的枚举 / 常量 / 契约文件 | 与文档冲突时**先提疑问，不要自行改文档也不要自行改代码** |

## 2. 不可逾越的红线

1. **禁止整文件重写**。只能用最小 diff 修改（新增/替换局部片段）。重构需求必须单独提出、单独一次改动。
2. **禁止顺手重构**。没有在本次任务范围内的代码，即使写得不好也不许动，只能记录下来汇报。
3. **禁止重复实现**。新增任何函数/组件/类之前，先搜索仓库是否已有等价实现；已有则复用或显式说明为何不用。
4. **禁止新增依赖**。技术选型以技术方案第 2.3 节为准，新增第三方库需先说明并获同意。
5. **禁止硬编码阈值**。倒计时、产能上限、SLA、留存天数、价格、文案一律走配置/字典/常量文件。
6. **禁止散落状态更新**。订单/格口/任务状态必须走统一状态机组件，禁止业务方法里直接 `update status`。
7. **禁止跳过留痕**。状态流转、开箱、影像访问、敏感操作必须写日志/记录表，日志只增不删。
8. **严格遵守范围边界**。加盟、柜机广告、家政增值、独立 App、智能调度属于 Out of Scope，不得进入 V1.0。

## 3. 变更流程（每次任务必须走）

1. **先读**：读本文件 + 相关文档 + 即将修改的文件全文。
2. **先复述**：开工前用 3-5 行说明「要改哪些文件、为什么、不动哪些」。若我的指令与既有约定冲突，先指出冲突再执行，不要默默改。
3. **最小改动**：一次任务只做一件事，改完即止。
4. **自校验**：改完必须过一遍第 5 节检查清单，把结果简短汇报。
5. **可回滚**：每个可运行的小步完成后提示我提交 git（一个功能点一次 commit，禁止混合提交）。

## 4. 单一事实来源（SSOT）

以下内容只允许有一处定义，任何第二处出现即为漂移，必须消除：

| 事项 | 唯一定义处 |
| --- | --- |
| 订单状态与流转 | 《订单状态机规则表.md》 + `OrderStatus` / `OrderEvent` / `OrderStatusTransitions`（三者必须逐条一致） |
| 错误码 | 后端统一错误码枚举 + 文档同步 |
| API 契约 | `openapi.yaml`（唯一真源）；前端 TS 类型由它生成，后端不得另立字段名，前端不得臆造字段 |
| 金额/时间格式 | 分 + BigDecimal；毫秒时间戳 + Asia/Shanghai |
| 站点/小区维度 | 所有业务表带 `site_id` / `community_id`，列表查询默认带站点过滤 |
| 权限标识 | PRD 6.7.3 的 `wash:*:*` 命名，不得另起一套 |
| 常量与字典 | 后端常量类 / 若依 dict，前端不得硬编码字典值 |

**新增或修改任何接口，必须先改 `openapi.yaml`，再改代码。** 顺序反了一律重做。

## 5. 完成前自检清单

- [ ] 是否复用了已有实现？是否新增了重复代码？
- [ ] 是否改了任务范围外的文件？（如有，必须显式列出并说明理由）
- [ ] 状态流转是否走了状态机？是否写了日志？
- [ ] 是否出现硬编码阈值、文案、字典值？
- [ ] 金额是否用分 + BigDecimal？时间是否为毫秒时间戳？
- [ ] 新增表是否带 `site_id` / `community_id` / 审计字段 / `del_flag`？
- [ ] 接口是否幂等（下单、支付回调、开箱回调、分配格口）？
- [ ] 是否通过类型检查 / lint / 现有测试？有无新增测试覆盖本次改动？
- [ ] 前端改动是否仍然对齐 PRD 页面清单与状态机映射？

## 6. 遇到以下情况先停下来问我

- 需求与既有文档/代码冲突；
- 发现既有实现有 bug 但不在本次任务范围；
- 需要新增依赖、新增表、修改公共组件或状态机定义；
- 有多种实现路径且代价差异较大。

## 7. 目录边界与端归属（防越界）

本项目是 **monorepo**：三端代码同一个仓库，但**依赖关系、构建、发布完全独立**，不要合并依赖，也不要拆仓库。

| 目录 | 端 | 技术栈 | 产物 | 运行端口 |
| --- | --- | --- | --- | --- |
| `carwash-mp` | C 端 / 师傅端（取送+作业合并）小程序 | uni-app + Vue3 + TS | `dist/build/mp-weixin` | 无（微信开发者工具导入） |
| `carwash-admin` | **运营后台前端**（尚未建） | RuoYi-Vue3 + Element Plus | `dist/` | 1024 |
| `carwash-server` | 后端（含若依原生模块 + `ruoyi-wash` 业务模块） | Spring Boot 4.1 + JDK17 | `ruoyi-admin/target/*.jar` | 8080 |
| `docker` | 本地中间件（MySQL 8 / Redis 7） | Compose | — | 3306 / 6379 |

**跨端改动的硬性顺序**（新增或修改接口时必须遵守）：

```
① openapi.yaml（契约）→ ② 后端 Controller/DTO → ③ 前端重新生成类型 → ④ 前端页面
```

**边界规则：**

1. **一次任务只动一端**。任务说改小程序，就不许碰后端；任务说改后端，就不许顺手改前端。
2. **跨端需求先声明**。确实需要同时改两端（如新增一个接口），开工前必须列出"要改哪几个端的哪几个文件"，不允许默默扩散。
3. **后台前端只能放 `carwash-admin`**，不得放进 `carwash-server`（后端目录里只允许有 Java / SQL / 配置）。
4. **依赖各自锁定**。禁止在仓库根建 `package.json`、禁止引入 npm/pnpm workspace、禁止把三端依赖合并——现阶段没有收益，只会增加耦合。
5. **按端提交**。一个 commit 只包含一个端的改动；跨端改动（如契约变更）单独一个 commit，并在 message 里写清影响哪些端。
6. **端口固定**，见上表。需要新增端口先更新本表。

## 8. 代码风格（与技术方案附录 C 一致）

- 后端：RuoYi 分层 Controller → Service → Manager → Mapper；跨模块只能调对方 Service/Manager，禁止跨模块直接调 Mapper。
- 前端：uni-app（Vue3 + TS + Pinia），接口统一走 `src/api`，请求拦截统一处理 token/错误/埋点。
- 命名：后端统一英文驼峰；前端组件 PascalCase；数据库下划线。
- 注释只写「为什么」，不写「做了什么」。
