# 夜间代客洗车平台 技术方案

| 项目 | 内容 |
| --- | --- |
| 文档名称 | 夜间代客洗车平台 技术方案（技术架构设计说明书） |
| 版本 / 状态 | V1.0 / 评审稿 |
| 编写日期 | 2026-09-15 |
| 上游文档 | 《洗车项目商业计划书》《洗车项目PRD V2.0》《对标产品设计稿（洗悦 X-CLEAN 22 张实测截图）》 |
| 产品范围 | ① C 端微信小程序 ② 智能钥匙柜物联系统 ③ 取送端小程序 ④ 中央站作业端 ⑤ 运营后台（RuoYi） |
| 目标读者 | 研发（后端 / 前端 / 硬件对接）、架构评审、测试、运维、AI 协作智能体 |

---

## 0. 修订记录

| 版本 | 日期 | 修订说明 |
| --- | --- | --- |
| V1.0 | 2026-09-15 | 基于商业计划书 + PRD V2.0 + 对标设计稿产出：总体架构、技术选型、状态机与物联设计、数据架构、接口与前端规范、安全合规、部署运维、成本与研发计划 |

---

## 1. 业务约束与技术设计原则

### 1.1 来自商业计划书的硬约束（技术必须服从）

| 约束 | 数值 | 技术含义 |
| --- | --- | --- |
| MVP 启动投资 | 约 10.5 万元 | 研发投入必须极小化：能买不自建、能单体不微服务、能托管不自运维 |
| 筹建周期 | 45 天内开业 | V0.5 阶段零研发试跑（厂商柜机小程序 + 群/表单接单），自研系统在开业后第 2-3 月交付 |
| 作业模型 | 2 名合伙人、1 中央站、2 台钥匙柜（12 格/台） | 无高并发；瓶颈是**产能排期与格口不超卖**，不是流量 |
| 单量目标 | 日均 7 单保本 → 15 单盈利 → 25-30 单封顶 | 全链路按「日订单 ≤ 数百」设计，10 万级订单量即可支撑 1-2 年 |
| 经营模式 | 夜间 19:00-次日 7:00 集中作业 | 定时任务集中在日间配置与结算，夜间只保「下单 / 开箱 / 打卡」链路可用性 |
| 复制节奏 | 1 年 6 个小区、3 年 5-8 点位 | 多站点（site_id）与多小区（community_id）字段 V1.0 必须带齐，避免 V2.0 重构 |

### 1.2 技术设计原则（P1-P9）

| 编号 | 原则 | 落地方式 |
| --- | --- | --- |
| P1 | 成本优先，单体优先 | 单体模块化架构（RuoYi 多模块），不上微服务、不上 K8s、不上消息中间件集群 |
| P2 | 每个环节产生不可篡改留痕 | 订单状态日志、开箱记录、SOP 打卡、影像全部只增不改，后台不可删除 |
| P3 | 配置化零发版 | 小区 / 柜机 / 服务 / 价格 / 时段 / 产能 / SOP / 工单类型 / 消息模板全部后台可配 |
| P4 | 状态流转收敛到统一状态机 | 禁止业务代码散落 update status；每次流转写 order_status_log |
| P5 | 并发正确性只保三个点 | 格口预占不超卖、产能不超卖、支付回调幂等——用分布式锁 + 乐观校验实现 |
| P6 | 外部依赖全部走适配层 | 柜机厂商、支付、短信、对象存储、地图均封装接口，可替换 |
| P7 | 硬编码阈值落参数表 | 支付倒计时、产能上限、SLA、留存天数等全部走若依 sys_config |
| P8 | 安全与合规按个保法最小化 | 手机号加密存储脱敏展示、影像鉴权水印、操作日志不可删 |
| P9 | 先跑通再优化 | V1.0 允许人工派单、规则提示；智能调度等算法放 V2.0 |

---

## 2. 总体架构

### 2.1 系统上下文

```
                 ┌────────────┐      ┌────────────┐      ┌────────────┐
                 │  车主 C 端  │      │ 取送人员端  │      │ 中央站作业端 │
                 │ 微信小程序  │      │ 微信小程序  │      │ 小程序/平板H5│
                 └─────┬──────┘      └─────┬──────┘      └─────┬──────┘
                       │ HTTPS             │                   │
                       ▼                   ▼                   ▼
                 ┌─────────────────────────────────────────────────────┐
                 │              夜间代客洗车平台（单体服务）              │
                 │   C端API ｜ 任务API ｜ 作业API ｜ 设备网关 ｜ 管理后台 │
                 └───┬──────────┬──────────┬──────────┬────────┬───────┘
                     │          │          │          │        │
        ┌────────────▼──┐ ┌─────▼─────┐ ┌──▼───────┐ ┌▼──────┐ ┌▼────────────┐
        │ 智能钥匙柜×2-6 │ │ 微信开放   │ │ 腾讯位置  │ │ 对象   │ │ 短信 / 企微  │
        │ 4G 联网厂商柜  │ │ 登录/支付  │ │ 服务(地图)│ │ 存储   │ │ 机器人(告警) │
        └───────────────┘ └───────────┘ └──────────┘ └────────┘ └─────────────┘
```

### 2.2 逻辑架构（分层）

```
┌────────────────────────── 端层（4 端 + 1 设备） ──────────────────────────┐
│ C端小程序  取送端小程序  作业端(平板H5/小程序)  运营后台Web  钥匙柜(4G)   │
└──────────────────────────────────┬──────────────────────────────────────┘
                                   │ HTTPS / TLS
┌──────────────────────────── 接入层 ─────────────────────────────────────┐
│ Nginx：TLS 终止、静态资源、反向代理、限流、CORS                          │
│ 鉴权：后台 JWT(若依) ｜ 小程序 wx-token ｜ 设备签名+IP白名单              │
└──────────────────────────────────┬──────────────────────────────────────┘
┌──────────────────────────── 应用层（RuoYi 多模块单体）───────────────────┐
│ 业务模块：wash-network / wash-goods / wash-order / wash-device /        │
│           wash-task / wash-customer / wash-ticket / wash-member /      │
│           wash-market / wash-finance / wash-media / wash-report        │
│ 公共能力：统一状态机 ｜ 产能与格口分配 ｜ 支付退款中心 ｜ 消息中心        │
│           文件服务 ｜ 埋点上报 ｜ 定时任务(quartz) ｜ 操作日志审计        │
└──────────────────────────────────┬──────────────────────────────────────┘
┌──────────────────────────── 数据层 ─────────────────────────────────────┐
│ MySQL 8（主库+每日备份） ｜ Redis 7（缓存/锁/倒计时/预占TTL）            │
│ 对象存储 COS/MinIO（照片/录像，带访问鉴权与留存策略）                    │
└─────────────────────────────────────────────────────────────────────────┘
┌──────────────────────────── 第三方适配层 ────────────────────────────────┐
│ DeviceAdapter(柜机厂商) ｜ WxPayAdapter ｜ WxMpAdapter ｜ SmsAdapter    │
│ MapAdapter(腾讯位置) ｜ StorageAdapter ｜ WecomBotAdapter               │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.3 技术选型

| 类别 | 选型 | 版本建议 | 选择理由 / 成本 |
| --- | --- | --- | --- |
| 后端框架 | RuoYi-Vue（Spring Boot 单体） | SB 3.2.x + JDK 17 | PRD 指定脚手架；自带用户/角色/菜单/部门/字典/参数/定时任务/操作日志，后台零成本起步 |
| 数据库 | MySQL | 8.0 | 云数据库或自建均可；日订单数百，单库够用 |
| 缓存 | Redis | 7.x | 格口预占 TTL、产能计数、分布式锁、支付倒计时、字典缓存 |
| 后台前端 | RuoYi-Vue3（Vue3 + Element Plus） | — | 与 PRD 菜单/权限标识体系对齐 |
| C 端 / 取送端 | uni-app（Vue3 + TS + Vite + Pinia） | HBuilderX / CLI | PRD 建议 uni-app 以便多端；取送端复用同一套请求/鉴权/埋点封装 |
| 作业端 | 同一 uni-app 工程编译 H5（平板横屏）+ 小程序 | — | W1/W2 看板与 SOP 打卡一套代码两端运行 |
| 地图 | 腾讯位置服务（对标同款） | JS SDK + 小程序 SDK | 机柜距离计算、导航、V2.0 站点地图 |
| 支付 | 微信支付 JSAPI v3 | — | V1.0 微信支付；余额/次卡 V1.1 |
| 消息 | 微信订阅消息 + 企微机器人 Webhook + 短信兜底（腾讯云 SMS） | — | 订阅消息模板一次性提全；运营告警走企微 |
| 对象存储 | 腾讯云 COS（备选 MinIO 自建） | — | 直传 + 临时密钥；照片 6 个月 / 录像 30 天生命周期规则 |
| 柜机 | 第三方智能柜厂商（12 格、4G、扫码开箱） | — | 采购合同锁定 API/回调/SLA/数据归属（PRD 第 10 章最大风险） |
| 部署 | 云服务器（Docker Compose）+ Nginx + HTTPS | 轻量 2C4G 起步 | 见第 9 章 |

---

## 3. 服务端架构设计

### 3.1 工程结构（Maven 多模块）

```
carwash-server
├── ruoyi-admin            # 启动模块 + 后台接口入口
├── ruoyi-framework        # 不改动（安全/拦截/数据源）
├── ruoyi-system           # 不改动（用户/角色/菜单/字典/参数）
├── ruoyi-common           # 通用工具
└── ruoyi-wash             # 业务父模块
    ├── wash-common        # 业务公共：状态机、错误码、BaseEntity、注解
    ├── wash-network       # 站点/小区/柜机/格口/物业
    ├── wash-goods         # 服务分类/服务项/价格方案/库存限量
    ├── wash-order         # 订单/支付/退款/产能排期/状态日志
    ├── wash-device        # 设备网关：注册/心跳/开箱鉴权/留痕/告警
    ├── wash-task          # 取车/洗车/还车任务、SOP、质检
    ├── wash-customer      # 客户/车辆/停放地址/标签
    ├── wash-ticket        # 工单/FAQ/保险理赔
    ├── wash-member        # 储值/余额/卡券（V1.1）
    ├── wash-market        # Banner/体验池/推荐/开通申请（V1.1）
    ├── wash-finance       # 对账/分成/成本（V1.1）
    ├── wash-media         # 影像与访问留痕
    └── wash-report        # 看板与日报（V1.1）
```

工程约定（承接 PRD 6.7.1）：
- 业务包 `com.ruoyi.wash.*`，不改 ruoyi-system / ruoyi-framework 核心代码。
- 所有业务表带 `site_id`、`community_id`、`create_by/create_time/update_by/update_time/del_flag`。
- 数据权限复用若依注解，站点 = 部门（dept），实现「站长只看本站」「物业只读本小区」。
- 单表/主子表优先若依代码生成，生成后补 site_id 与逻辑删除。
- 金额一律 `BigDecimal`、单位分；时间戳统一 Asia/Shanghai。

### 3.2 分层规范

```
Controller（参数校验/鉴权注解） → Service（业务编排/事务） → Manager（跨域调用/缓存）
→ Mapper（MyBatis，单表简单 SQL）
```

- Service 层禁止跨模块直接调用 Mapper，跨领域必须走对方 Service/Manager，为 V2.0 拆分预留边界。
- 写操作统一 `@Transactional`；状态流转方法必须经过状态机组件。

### 3.3 统一状态机设计（核心组件）

**范围**：订单（15 态）、格口（5 态）、取车/洗车/还车子任务、工单。

流转规则详见《订单状态机规则表.md》（SSOT），代码侧对应 `com.ruoyi.wash.common.statemachine` 包。

**实现方式**：自研轻量「流转表驱动」状态机（不引入 Spring StateMachine，降低复杂度）：

```java
// wash-common/statemachine
OrderStateMachine.fire(orderId, from, event, context)
  1. Redis 加锁 lock:order:{orderId}（TTL 10s，防并发双流转）
  2. SELECT ... FOR UPDATE 读取当前状态（乐观二次校验）
  3. 查流转规则表（内存缓存）确认 event 合法，非法则抛 OrderStatusException
  4. 执行流转前置 Action（如释放格口、退名额、发消息）
  5. UPDATE 订单状态（WHERE status = from，affected=0 则并发冲突重试/报错）
  6. 插入 wash_order_status_log（from/to/操作者/来源端/原因/时间）
  7. 执行流转后 Action（推送订阅消息、生成子任务、企微告警）
```

关键事件与规则对齐 PRD 附录 A：`WAIT_PAY → WAIT_KEY → KEY_IN → PICKING → TO_STATION → WASHING → QC →(不通过回退 WASHING) → WAIT_RETURN → RETURNING → RETURNED → WAIT_REVIEW → FINISHED`；任一状态 → `CANCELED → REFUNDING → REFUNDED`；`WAIT_KEY` 超时顺延次日。

来源端（source）枚举：`C_APP / PICK_APP / STATION_APP / ADMIN / JOB / DEVICE`，与日志表一一对应，保证 C 端时间轴 = 日志记录本身（PRD 4.1 设计原则）。

### 3.4 产能与格口分配（并发正确性设计）

本项目仅有的三个并发热点，全部收敛在 wash-order / wash-device：

| 热点 | 方案 |
| --- | --- |
| 产能不超卖（每晚 ≤ N 单） | Redis `capacity:{siteId}:{date}` 原子 `DECR` 抢名额；提交与支付前双校验；支付超时/取消 `INCR` 回补；DB 侧每日名额表做兜底对账 |
| 格口不超卖（一物一码） | 提交时 `lock:cabinet:{cabinetId}` 内完成「查空闲格口 → UPDATE slot SET status=RESERVED, order_id=?, expire_time=? WHERE status=FREE LIMIT 1」；TTL 15 分钟与支付倒计时一致，超时由定时任务 + 支付回调双路径释放 |
| 支付回调幂等 | 回调表唯一键 `trade_no` + 订单号状态机校验（仅 WAIT_PAY 可入账）；签名校验失败直接拒绝并告警；回调丢失靠主动查单补偿任务（每 5 分钟扫描待确认订单） |

### 3.5 定时任务清单（复用若依 quartz）

| 任务 | 频率 | 说明 |
| --- | --- | --- |
| 支付超时取消 | 每 1 分钟 | WAIT_PAY 超 15 分钟取消，释放格口与名额 |
| 格口预占释放 | 每 5 分钟 | 清理过期 RESERVED |
| 主动查单补偿 | 每 5 分钟 | 支付回调丢失场景，调微信查单接口 |
| 柜机离线检测 | 每 5 分钟 | 心跳超 10 分钟标记离线并企微告警 |
| 任务阻塞告警 | 每 10 分钟 | 取车/洗车/还车任务超阈值在异常看板标红 |
| 未存钥匙顺延 | 每日 23:30 | WAIT_KEY 顺延次日 + 推送 + 权益返还 |
| 承诺还车超时 | 每日 04:30 / 07:00 | 生成工单并按规则补偿 |
| T+1 对账 | 每日 02:00 | 生成对账单，差异标红生成工单 |
| 影像清理 | 每日 03:00 | 按留存策略（照片 6 个月 / 录像 30 天）归档删除 |
| 数据日报 | 每日 08:00 | 推送昨日经营日报至企微 |
| 券到期提醒 | 每日 10:00 | 到期前 3 天推送（V1.1） |

### 3.6 消息中心设计

- 统一入口 `MessageService.send(scene, bizId, receiver)`，场景枚举对齐 PRD 6.5（支付成功、请存钥匙、钥匙已收到、清洗完成、已还车、顺延、取消、退款、告警…）。
- 渠道路由：C 端用户 → 订阅消息（主）+ 短信（兜底，仅还车通知）；运营 → 企微机器人 + 后台站内信；紧急异常 → 企微 + 短信 + 电话升级（SLA 15 分钟）。
- 模板内容存参数表（变量占位符），支持按站点差异化；同一订单同一节点 5 分钟内去重（Redis `dedup:msg:{orderId}:{node}` TTL 300s）。
- 订阅消息模板在小程序上线前**一次性提全**（下单/进度/还车/评价/卡券到期等），避免后续审核排队。

---

## 4. 智能钥匙柜（物联）接入设计

### 4.1 接入模式与适配层

V1.0 采用「第三方智能柜 + 厂商开放 API/SDK」，自研侧只做**网关与业务编排**，不做柜机固件。

```
柜机(4G) ⇄ 厂商云 ⇄ HTTPS回调/开放API ⇄ wash-device 网关
                                        ├─ SignatureFilter（签名+时间戳+IP白名单+限流）
                                        ├─ IdempotentFilter（callbackId 幂等表，唯一键）
                                        ├─ DeviceAdapter 接口（厂商可替换）
                                        │    register / heartbeat / slotSync /
                                        │    allocate / openBox / openBoxCallback /
                                        │    doorStateCallback / alarm / emergencyOpen
                                        └─ 异步落库（MQ 解耦可选，V1.0 用线程池+重试表）
```

**适配层接口（DeviceAdapter）**是 V1.0 最关键的技术抽象，原因：
1. 厂商接口能力不足是 PRD 第 10 章认定最大风险；适配层保证可换厂商（备选厂商预研成本 ≈ 1 个实现类）。
2. V0.5 过渡期若直接用厂商原生小程序，适配层可先空实现，接口契约不变，V1.0 无感切换。
3. 采购合同必须锁定：API/SDK 能力、回调清单、SLA、数据归属（开箱记录回传我方后台）。

### 4.2 设备侧接口契约（对齐 PRD 附录 B）

| 接口 | 方向 | 关键字段 |
| --- | --- | --- |
| 设备注册/心跳 | 柜机→平台 | deviceNo, communityId, position, slotTotal, heartbeatTime, status（5 分钟/次） |
| 格口状态同步 | 双向 | slotNo, status(FREE/RESERVED/OCCUPIED/OCCUPIED_BACK/DISABLED), orderId |
| 分配格口 | 平台→柜机 | orderId, slotNo, ttl |
| 开箱指令与回调 | 双向 | code(取件码/工号), role(客户/员工/管理员), userId, result, time |
| 开关门状态上报 | 柜机→平台 | slotNo, action(deposit/pickup), time —— **用于自动流转订单** |
| 故障告警 | 柜机→平台 | alarmType(断网/断电/卡口), deviceNo, slotNo |
| 应急开箱 | 平台→柜机 | adminUser, reason, slotNo（二次鉴权 + 强制留痕） |

### 4.3 开箱鉴权与留痕

| 角色 | 凭证 | 校验 |
| --- | --- | --- |
| 客户（存/取钥匙） | 6 位取件码或扫码 | 一次有效、绑定订单与人；连错 3 次锁定 5 分钟并通知站长 |
| 取送人员 | 工号 + 扫码 | 校验人员与任务绑定关系，非绑定请求 100% 拒绝并告警 |
| 管理员（应急） | 二次密码/审批 | 强制填原因，写入开箱记录并高亮 |

每次开箱写 `wash_open_log`（柜机/格口/订单/角色/人员/动作/结果/来源端），只读不可删除；柜门 60 秒未关闭自动关合并提示；离线时走「备用取件码 + 客服远程协助 + 应急机械钥匙」三级兜底。

---

## 5. 数据架构

### 5.1 核心表清单（V1.0 P0 ≈ 22 张，字段对齐 PRD 附录 D）

**网络与设备**：`wash_station`（中央站）、`wash_community`（小区，含 access_auth 通行授权）、`wash_cabinet`（柜机）、`wash_slot`（格口，lock_code）、`wash_device_alarm`
**商品**：`wash_category`、`wash_service`、`wash_service_price`（站点价/活动价）、`wash_capacity`（每日产能名额）
**订单**：`wash_order`、`wash_order_item`、`wash_order_status_log`、`wash_payment`、`wash_refund`
**任务与 SOP**：`wash_pick_task`、`wash_wash_task`、`wash_return_task`、`wash_sop`（模板）、`wash_sop_record`、`wash_qc_record`、`wash_task_exception`
**客户**：`wash_customer`、`wash_vehicle`、`wash_address`
**影像与工单**：`wash_media`、`wash_media_access_log`、`wash_ticket`、`wash_ticket_type`、`wash_faq`
**V1.1 增量**：`member_account`、`wash_recharge`、`balance_log`、`coupon_tpl`、`wash_coupon`、`wash_banner`、`trial_pool`、`referral`、`community_apply`、`wash_settlement`、`share_bill`、`insurance`

### 5.2 关键索引与查询

| 表 | 索引 | 支撑场景 |
| --- | --- | --- |
| wash_order | `uk order_no`；`idx(site_id, status, appoint_time)`；`idx(customer_id)`；`idx(cabinet_id, slot_id)` | 订单列表三 Tab、当日排产、格口反查 |
| wash_slot | `uk(cabinet_id, slot_no)`；`idx(status, expire_time)` | 格口分配、预占释放扫描 |
| wash_order_status_log | `idx(order_id, create_time)` | C 端 8 节点时间轴（只读追加） |
| wash_open_log | `idx(cabinet_id, create_time)`、`idx(order_id)` | 开箱追溯、异常高亮 |
| wash_payment | `uk trade_no` | 回调幂等 |
| wash_media | `idx(biz_type, biz_id)` | 订单详情证据区 |

容量评估：日均 25 单 × 365 ≈ 1 万单/年，全表 10 万级，单库 + 上述索引绰绰有余，无需分库分表。

### 5.3 缓存设计（Redis Key 规范）

| Key | 类型 / TTL | 用途 |
| --- | --- | --- |
| `capacity:{siteId}:{date}` | int，当日 23:59 过期 | 产能名额原子扣减/回补 |
| `lock:order:{orderNo}` | string，10s | 状态机分布式锁 |
| `lock:cabinet:{cabinetId}` | string，5s | 格口分配锁 |
| `slot:reserve:{slotId}` | string，900s | 格口预占 TTL（与支付倒计时一致） |
| `dict:{dictType}` | hash，1h | 若依字典缓存（延迟双删保证一致性） |
| `service:price:{siteId}` | hash，60s | 服务价格（后台改价 60 秒内生效） |
| `wx:token:{openid}` | string，7d | 小程序会话 |
| `dedup:msg:{orderId}:{node}` | string，300s | 消息去重 |
| `idem:callback:{callbackId}` | string，24h | 回调幂等 |

一致性策略：缓存与 DB 采用延迟双删；状态类数据（订单状态、格口状态）一律以 DB 为准，缓存仅加速读。

### 5.4 文件与影像

- 上传链路：小程序直传 COS（服务端签发临时密钥，限制目录/大小/类型）→ 回传 fileId → 落 `wash_media`。
- 照片规格：下单停放照 ≤3 张/5MB、取车 360° ≥6 张（四角+前后+仪表盘里程/电量）、洗后照 + 自动拼接前后对比图。
- 访问控制：临时 URL + 有效期；查看加水印（访问人+时间）；下载与查看写 `wash_media_access_log`；禁止后台删除。
- 留存策略：照片 ≥6 个月、录像 ≥30 天（COS 生命周期规则 + 影像清理任务双保险）。

### 5.5 数据一致性要点

| 场景 | 机制 |
| --- | --- |
| 支付回调 × 订单状态 | 唯一约束 + 状态机前置校验 + 主动查单补偿 |
| 柜机回调 × 订单流转 | 回调幂等表 + 门状态上报驱动流转，开箱失败可重试不脏状态 |
| 名额/格口 × 超时释放 | 定时任务与业务回调双路径释放，幂等 |
| 每日对账 | T+1 对账任务核对微信账单 vs 订单 vs 名额/格口，差异标红生成工单 |

---

## 6. 接口设计规范

### 6.1 通用规范

- REST 风格，路径 `/api/v1/...`（小程序侧）与 `/admin-api/...`（若依后台）；设备回调 `/device-callback/...`。
- 统一响应体 `{ code, msg, data }`；错误码分段：A0xxx 通用 / B1xxx 订单 / B2xxx 格口柜机 / B3xxx 支付 / B4xxx 任务 / C0xxx 第三方；错误码表随代码维护并在 wiki 同步。
- 分页：pageNum/pageSize + total；列表默认按 createTime 倒序。
- 时间戳毫秒；金额分（前端展示除以 100）。
- 幂等：所有写接口支持 `Idempotency-Key` 请求头（下单、支付、开箱必传）。

### 6.2 三套鉴权体系

| 通道 | 主体 | 机制 |
| --- | --- | --- |
| 后台 Web | 员工账号 | 若依 JWT + 菜单/按钮权限（`wash:*:*` 标识）+ 数据权限（dept=站点） |
| 小程序（C 端/师傅端） | 微信用户 | `code2session` 换 openid/unionid → 自签 token；手机号用微信快速验证组件；B 端人员再校验角色与站点绑定 |
| 设备回调 | 柜机厂商 | 签名（appKey+secret+时间戳）+ IP 白名单 + 限流 + 回调幂等 |

### 6.3 核心接口清单（V1.0 摘要）

| 端 | 接口（节选） |
| --- | --- |
| C 端 | 登录授权；站点/小区切换与开通申请；服务分类与列表（含已约满态）；机柜列表（空闲数/距离）；提交订单（预占格口+产能校验）；订单列表（三 Tab）；订单详情（时间轴+证据）；开箱码获取；紧急取钥匙；取消订单；车辆 CRUD；评价；售后/重洗/退款/理赔申请；FAQ 与电话配置 |
| 取送端 | 任务池（待接单/我的/已完成）；接单/派单确认；取钥匙打卡；取车照片上传（≥6 张强校验）；到站打卡；还车打卡（停放照+归柜）；异常上报（无法启动/剐蹭）；工作量统计 |
| 作业端 | 工位队列看板；SOP 步骤打卡（逐步耗时+关键步骤照片）；质检提交（对比图）；打回重洗；产能预警查询 |
| 后台 | 模块 1-7 的 CRUD + 订单干预（取消/改派/退款审核，强制原因）+ 产能排期/一键停业 + 柜机监控与应急开箱 + 开箱记录 + SOP 模板 + 数据字典参数 |
| 设备 | 注册/心跳、格口状态同步、开箱回调、开关门上报、故障告警、应急开箱指令 |

---

## 7. 前端设计

### 7.1 工程结构

```
carwash-mp          # uni-app 单仓（Vue3 + TS + Vite + Pinia + uni-ui）
├── src/pages/c     # C 端页面（P1-P16，对齐 PRD 6.1.2 页面清单）
├── src/pages/pick  # 取送端页面（T1 任务池 / T2 打卡）
├── src/pages/station # 作业端页面（W1 看板 / W2 SOP）
├── src/api         # 接口封装（按模块分文件）
├── src/stores      # Pinia：user / order / site
├── src/utils       # request 拦截（token/埋点/错误统一处理）、upload、track
└── src/static
carwash-admin-ui    # RuoYi-Vue3（Element Plus），按 6.7.3 菜单树组织 views
```

### 7.2 关键实现要求

| 主题 | 要求（承接 PRD 6.1.1 全局规范） |
| --- | --- |
| 登录 | 未登录可浏览首页/服务列表；下单/我的触发授权；禁止未授权采集手机号；首次弹协议，未同意不可用 |
| 状态展示 | C 端时间轴 8 节点数据 = `order_status_log` 接口返回，禁止前端写死进度文案；进行中订单 60s 轮询 + onShow 刷新 |
| 主按钮 | 订单详情/卡片主按钮随状态机映射（去支付/去存钥匙/查看进度/去取钥匙/去评价），同屏 1 主 + 2 次 |
| 金额 | 三档价（会员价>活动价>原价）优先级前端统一实现；倒计时以服务器时间为准 |
| 敏感操作 | 取消/退款/删车/紧急取钥匙二次确认弹窗；支付/开箱失败给可执行下一步（重试/客服/应急开箱） |
| 拍照上传 | 直传对象存储带进度与失败重试；取送端照片不足禁止提交；弱网本地暂存恢复后补传 |
| 埋点 | request 拦截器统一上报 page_view 与 PRD 第 8 章事件表（order_submit / pay_result / capacity_block / key_deposit / qc_result 等） |
| 性能 | 首屏 ≤2s：分包加载（C/Pick/Station 三分包）、图片 CDN+压缩、骨架屏、接口 <3s 提示 |
| 兼容 | 微信 8.0+；作业端平板横屏适配；后台 Chrome/Edge |

---

## 8. 安全与合规设计

| 类别 | 措施 |
| --- | --- |
| 传输 | 全链路 HTTPS（TLS1.2+），后台强跳 HTTPS，HSTS |
| 数据加密 | 手机号 AES 加密存储 + 展示脱敏（138\*\*\*\*8888）；导出需审批并留痕 |
| 访问控制 | 三套鉴权隔离；权限矩阵对齐 PRD 附录 C；越权测试纳入验收 |
| 敏感操作 | 应急开箱、退款、改价、停业等二次校验（密码/审批）+ 操作日志不可删除 |
| 影像安全 | 临时 URL + 水印 + 访问留痕；车牌照片是否打码走后台开关（默认不打码以便核对） |
| 支付安全 | v3 平台证书验签 + 回调幂等 + 主动查单；金额后端计算，前端金额仅展示 |
| 合规 | 个保法最小化收集（定位拒绝可降级、相册仅用于照片上传）；注销账号脱敏留存；V1.0 不做储值（预付费合规风险），V1.1 上线前完成资金存管评估 |
| 小程序合规 | 类目资质提前准备（营业执照），预留 7-10 天审核缓冲；订阅消息模板一次提全；遵守平台支付与诱导分享规范 |

---

## 9. 部署与运维

### 9.1 环境与拓扑

| 环境 | 用途 | 配置 |
| --- | --- | --- |
| dev | 本地开发 | 本地 MySQL/Redis + 微信测试号 |
| test | 联调/验收 | 云上单机，独立库 + 厂商沙箱/真机柜 1 台 |
| prod | 生产 | 云服务器单机 + 每日备份 |

**生产拓扑（V1.0）**：

```
云服务器（轻量 2C4G，佛山/广州地域）
 ├─ Docker Compose：nginx ｜ carwash-server(JDK17) ｜ mysql 8 ｜ redis 7
 ├─ COS：影像（生命周期规则）+ 前端静态资源 + CDN（可选）
 └─ 云监控/告警 → 企微机器人
```

- 可用性：下单与开箱链路 ≥99.9%；服务器故障兜底 = 柜机本地应急开箱 + 人工微信接单流程（商业计划书「系统故障」预案）。
- 备份：MySQL 每日全量 + binlog，保留 30 天，异机存放；每季度做一次恢复演练。
- 发布：`git tag → 脚本构建 Docker 镜像 → test 验证 → prod 滚动重启`；数据库变更用 Flyway 版本化脚本，先备份后执行；小程序端走体验版灰度 → 提审 → 全量。

### 9.2 可观测性

| 层 | 内容 |
| --- | --- |
| 日志 | Logback 按天滚动；请求日志含 traceId（MDC），订单全链路以 orderNo 串联 |
| 指标 | 下单成功率、支付成功率、开箱成功率、回调失败率、柜机在线率、核心接口 P95 |
| 告警 | 阈值告警 + 业务告警（柜机离线、任务阻塞、超产能、工单 SLA 超时）统一推企微机器人；15 分钟未响应升级站长 |
| 对账 | T+1 对账任务输出差异表，差异必生成工单闭环 |

---

## 10. 成本估算（对齐商业计划书预算）

### 10.1 运行成本（月度）

| 项目 | 方案 | 月成本（元） |
| --- | --- | --- |
| 云服务器 | 轻量 2C4G（3 年付更优） | 100-200 |
| 域名 + SSL | .com + 免费/年付证书 | 5-10 |
| 对象存储 + 流量 | 照片录像按量 + 生命周期 | 20-60 |
| 短信（还车通知兜底） | 腾讯云 SMS 按条 | 10-30 |
| 地图 | 腾讯位置服务免费配额 | 0 |
| 小程序认证 | 300 元/年 | 25 |
| 合计 | | **约 160-325 元/月** |

落在商业计划书「物联网/系统 100-200 元/月」量级（物联网卡费用含在钥匙柜厂商方案内）。

### 10.2 研发投入（V1.0）

| 角色 | 投入 | 说明 |
| --- | --- | --- |
| 后端（含架构） | 1 人 × 6-8 周 | 状态机、设备网关、订单产能为核心难点 |
| 前端 | 1 人 × 5-6 周 | 三端 uni-app + 后台二开 |
| UI | 0.5 人 × 2 周 | 对标设计稿体系 + 品牌色（青蓝系） |
| 测试 | 兼职 × 2 周 | PRD 第 12 章 20 单全流程穿测 |

> PRD 明确「面向多智能体协作开发」，可将 C 端页面、后台 CRUD、单表代码生成等交给 AI 智能体批量实现，人工聚焦状态机、格口并发、设备网关、支付回调四个高风险点。V0.5 阶段（第 1-45 天）零研发投入，用厂商小程序 + 群/表单 + 共享表格跑业务，自研系统并行开发。

---

## 11. 研发里程碑（对齐 PRD 第 11 章版本规划）

| 阶段 | 时间 | 技术交付 |
| --- | --- | --- |
| V0.5 开业过渡 | 第 1-45 天 | 无自研线上系统；厂商柜机小程序 + 企微机器人 + 共享表格排期；**并行完成**：架构与库表设计评审、设备网关对接联调（厂商沙箱+真机）、后台模块 1-7 骨架 |
| V1.0 MVP | 第 2-3 月 | C 端（P1-P11/P13/P16）+ 柜机接入 + 取送端 + 作业端 + 后台模块 1-7 + 状态机/支付/消息/文件公共能力；20 单全流程穿测；上线 |
| V1.1 增长 | 第 4-6 月 | 储值/卡券/营销/财务/看板（模块 8-13）、多柜机多小区扩展、耗材登记、工单增强 |
| V2.0 规模化 | 第 6-12 月 | 多站点/多城市、加盟账号与开放 API（wash-openapi）、站点地图、智能调度、易耗品商城 |

**验收口径**（承接 PRD 第 12 章）：P0 用例 100% 通过；≥20 单真实穿测覆盖退款/重洗/紧急取钥匙/柜机离线/超产能顺延；开箱端到端成功率 ≥99%、支付成功率 ≥98%；埋点与看板数据误差 0；越权与备份恢复演练通过。

---

## 12. 技术风险与对策

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| 柜机厂商接口能力不足（最大风险） | 透明化卖点落空 | 采购合同锁定 API/回调/SLA/数据归属；DeviceAdapter 抽象可换厂商；V0.5 厂商小程序过渡；备选厂商预研 |
| 支付回调丢失/重复 | 资金差异 | 验签+幂等+主动查单补偿+T+1 对账差异工单 |
| 格口/产能超卖 | 承诺还车违约 | 锁内分配 + 原子计数 + 双路径释放 + 每日兜底对账 |
| 夜间作业打卡不落地 | 信任链路断裂 | 移动端一步一动作、照片必填强校验、未打卡无法流转、阻塞告警 |
| 小程序审核延期 | 上线延期 | 提前办类目资质、订阅消息一次提全、预留 7-10 天缓冲 |
| 需求蔓延 | 拖慢 MVP | 严格执行 PRD Out of Scope（加盟/广告/家政/独立 App/智能调度不入 V1.0），新增进需求池 |
| 单人服务器故障 | 夜间链路不可用 | 每日备份+快速重建脚本；柜机应急开箱+人工接单兜底 |

---

## 附录 A：关键架构决策记录（ADR 摘要）

| # | 决策 | 备选 | 结论理由 |
| --- | --- | --- | --- |
| ADR-1 | 单体模块化（RuoYi 多模块），不上微服务 | Spring Cloud | 预算 10.5 万、2 人研发、日订单数百；模块边界按领域划分即可支撑 V2.0 拆分 |
| ADR-2 | 自研轻量状态机 | Spring StateMachine / Seata | 流转规则可控、日志原生落库、无框架学习成本 |
| ADR-3 | 柜机走厂商 API + 自研网关适配层 | 自研柜机硬件 | 硬件自研远超预算；适配层是唯一可控的替换点 |
| ADR-4 | C 端用 uni-app | 微信原生小程序 | 三端（C/取送/作业）复用请求/鉴权/埋点；作业端可编译 H5 适配平板 |
| ADR-5 | 腾讯云 COS + 临时密钥直传 | 自建 MinIO / 后端中转 | 零运维、生命周期规则天然匹配留存策略；MinIO 保留为私有化备选 |
| ADR-6 | 名额/格口用 Redis + DB 双层控制 | 仅 DB 行锁 | 峰值低但正确性关键；Redis 原子操作 + DB 约束双保险，实现简单 |
| ADR-7 | V1.0 不做储值 | 直接上线充值 | PRD 合规要求：预付费资金合规评估完成前不开放（V1.1） |

## 附录 B：C 端时间轴节点与状态映射（前后端契约）

| 时间轴节点 | 状态 | 证据 |
| --- | --- | --- |
| 已下单 | WAIT_PAY 之后 | 订单号 |
| 钥匙已入柜 | KEY_IN | 开箱记录（人/时/格号） |
| 已取车 | PICKING→TO_STATION | ≥6 张 360° 照片 + 仪表盘 |
| 到达中央站 | WASHING 前 | 入场时间、工位号 |
| 清洗中 | WASHING | SOP 进度 |
| 质检完成 | QC 通过 | 洗后照 + 前后对比图 |
| 已还车 | RETURNED | 停放照 + 归柜记录 |
| 待取钥匙 | RETURNED→WAIT_REVIEW | 取件码 |

## 附录 C：约定清单（研发必读）

1. 所有写状态的地方必须走 `OrderStateMachine` / `SlotStateMachine` / `TaskStateMachine`，违者评审打回。
2. 所有业务表带 `site_id`，所有列表查询默认带站点过滤（数据权限自动注入）。
3. 所有阈值（倒计时、产能上限、SLA、留存天数）从 `sys_config` 读取，禁止硬编码。
4. 金额单位分、BigDecimal；时间毫秒时间戳、Asia/Shanghai。
5. 影像与日志只增不删；删除类接口一律不开发。
6. 外部依赖（柜机/支付/短信/地图/存储）只允许通过 wash-common 中定义的 Adapter 接口调用。
7. 提交订单、支付回调、开箱回调、分配格口四类接口必须幂等。
